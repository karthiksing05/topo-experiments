{
  "_comment": [
    "All top-level keys can be overridden via CLI (e.g. --epochs 50 --device cuda:1).",
    "null paths resolve to project-root defaults at runtime.",
    "Per-layer keys (topo_scale, factor_h, factor_w, lambda_kl, lambda_entropy, temperature)",
    "must be edited here; they cannot be set from the command line.",
    "temperature: softmax temperature for KL-divergence and entropy computation.",
    "Only fc1 receives TopoLoss + sparsity penalties; fc2 is left unconstrained."
  ],

  "data_dir":         null,
  "output_dir":       null,
  "hidden_size":      256,
  "epochs":           20,
  "batch_size":       128,
  "lr":               0.0005,
  "device":           "cuda:0",
  "print_freq":       5,

  "resume_topo":      null,
  "resume_topo_only": null,
  "resume_base":      null,

  "layers": {
    "fc1": {
      "topo_scale":     10.0,
      "factor_h":        4.0,
      "factor_w":        4.0,
      "lambda_kl":       2.0,
      "lambda_entropy":  5.0,
      "temperature":     1.0
    }
  }
}
